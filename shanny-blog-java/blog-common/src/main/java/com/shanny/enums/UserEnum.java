package com.shanny.enums;
public class UserEnum {
    public enum UserSex{
        UNKNOWN,
        MAN,
        FEMALE
    }
    public enum UserStatus{
        ACTIVE,
        LOCKED,
        DELETED
    }
    public enum UserType{
        AUTHOR,
        USER,
    }
}
